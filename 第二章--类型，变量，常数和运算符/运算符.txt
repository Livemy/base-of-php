1.  错误控制运算符 
   当我们在表达式的前面加上错误控制运算符@时，表达式可能产生的错误信息将会被忽略。即不会被警告。

2.  执行运算符
    执行运算符（'')的用途是执行shell命令。

3.  输出函数
    print str;只能输出一个字符串（str);
    var_dump(var1[,var2[,var3...]]); 可以输出一个或多个变量    var_dump($a,$b,$c);